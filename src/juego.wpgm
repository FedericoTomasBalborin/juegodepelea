import wollok.game.*
import plataformas.*
import personajes.*

program juego
{
	game.title("Extreme Shooters")
	game.height(10)
	game.width(20)
	//fondo.background()
	escenarioUno.creoPlataformas()
	jugador.controles()
	jugador.escogerPersonaje()	
	jugador2.controles()
	jugador2.escogerPersonaje()
	game.onCollideDo(jugador.personaje(),{piso => piso.subir(jugador.personaje())})
	game.onCollideDo(jugador2.personaje(),{piso => piso.subir(jugador2.personaje())})
	game.start()
}